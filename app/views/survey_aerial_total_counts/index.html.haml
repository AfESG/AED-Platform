%h1= t('.title')
%p= link_to t('.new'), new_survey_aerial_total_count_path

%table
  %thead
    %tr
      %th= SurveyAerialTotalCount.human_attribute_name :population_submission_id
      %th= SurveyAerialTotalCount.human_attribute_name :surveyed_at_stratum_level
      %th= SurveyAerialTotalCount.human_attribute_name :stratum_level_data_submitted
      %th{:colspan => 3}= t 'actions'
  
  %tbody
    - for survey_aerial_total_count in @survey_aerial_total_counts
      %tr{ :class => cycle(:odd, :even) }
        %td= survey_aerial_total_count.population_submission_id
        %td= survey_aerial_total_count.surveyed_at_stratum_level
        %td= survey_aerial_total_count.stratum_level_data_submitted
        %td.action= link_to t('show'), survey_aerial_total_count
        %td.action= link_to t('edit'), edit_survey_aerial_total_count_path(survey_aerial_total_count)
        %td.action= link_to t('destroy'), survey_aerial_total_count, :confirm => t('are_you_sure'), :method => :delete
