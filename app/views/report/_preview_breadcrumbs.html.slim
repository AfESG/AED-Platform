- year      = local_assigns[:year] || @year
- continent = local_assigns[:continent] || @continent
- site      = local_assigns[:site] || @site
- region    = local_assigns[:region] || @region
- country   = local_assigns[:country] || @country


ol.breadcrumb
  li = link_to 'Reports', report_path
  li = year
  li = link_to_unless region.nil? && site.nil?, continent, "/preview_report/#{@filter}/#{year}/#{continent}"
  - if region
    li = link_to_if country, region, "/preview_report/#{@filter}/#{year}/#{continent}/#{region.gsub(' ','_')}"
    - if country
      li = country
  - elsif site
    li = site
