- year      = local_assigns[:year] || @year
- continent = local_assigns[:continent] || @continent
- region    = local_assigns[:region] || @region
- country   = local_assigns[:country] || @country
- survey    = local_assigns[:survey] || @survey_zone

ol.breadcrumb
  li = link_to 'Reports', report_path
  - if year
    li = link_to_if continent, year, "/report/#{year}"
  - if continent
    li = link_to_if region, continent, "/report/#{year}/#{continent}"
  - if region
    li = link_to_if country, region, "/report/#{year}/#{continent}/#{region.gsub(' ','_')}"
  - if country
    li = link_to_if survey, country, "/report/#{year}/#{continent}/#{region.gsub(' ','_')}/#{country.gsub(' ','_')}"
  - if survey
    li = "#{survey['survey_zone']} #{survey['designate']}"
